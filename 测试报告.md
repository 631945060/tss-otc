# 测试报告

测试对象为本目录的 Go 服务端。运行 `go test ./... -count=1 -v` 可重复执行接口分层测试、tss-lib 参数核验与 3 节点 DKG/2 节点协作签名测试。

`routes/tss_wallet_route_test.go` 通过 Gin 路由入口验证统一响应体、请求校验和业务状态机，覆盖如下接口与结果。

| 编号 | 接口或模块 | 输入/步骤 | 预期结果 | 结论 |
|---|---|---|---|---|
| T01 | `GET /api/v1/health` | 查询服务状态 | 返回 2-of-3 参数 | 通过 |
| T02 | `GET /api/v1/wallets` | 查询初始钱包 | 返回钱包元数据 | 通过 |
| T03 | `POST /api/v1/transactions` | 钱包、收款方、金额、摘要 | 创建待审批交易 | 通过 |
| T04 | `POST /api/v1/sign-sessions` | 绑定交易、钱包和摘要 | 创建 2-of-3 会话 | 通过 |
| T05 | `POST /sign-sessions/:id/approve` | `node-1` 审批 | 会话进入 signing | 通过 |
| T06 | `POST /sign-sessions/:id/approve` | `node-2` 审批 | 会话成功，交易签署 | 通过 |
| T07 | 重复审批 | 同一节点再次审批 | 返回 1009 状态冲突 | 通过 |
| T08 | `GET /api/v1/transactions/:id` | 查询已签署交易 | 返回交易详情 | 通过 |
| T09 | `GET /api/v1/audit-logs` | 查询审计记录 | 返回创建、审批、完成事件 | 通过 |
| T10 | 会话参数校验 | 空请求创建会话 | 返回 1001 参数错误 | 通过 |
| T11 | 钱包查询异常 | 查询不存在的钱包 | 返回 1004 不存在 | 通过 |
| T12 | `GET /api/v1/participants` | 查询参与节点 | 返回 3 个节点 | 通过 |
| T13 | `POST /api/v1/nodes/heartbeat` | 节点心跳上报 | 节点版本和时间更新 | 通过 |
| T14 | `POST /participants/:id/refresh` | 发起重新共享请求 | 创建 pending epoch 元数据 | 通过 |
| T15 | `GET /api/v1/system/metrics` | 查询运行指标 | 返回会话、节点、交易指标 | 通过 |
| T16 | `internal/tssconfig` | 参数测试 | `threshold=1` 映射 2-of-3 | 通过 |
| T17 | `internal/tssprotocol` | 3 节点 DKG，任选 2 节点签名 | ECDSA 验签通过 | 通过 |
| T18 | 并发会话创建 | 10/25/50/100 并发请求 | 无失败并输出性能指标 | 通过 |

业务接口的审批状态机只处理会话授权与审计关联；真实门限密码协议由 `internal/tssprotocol` 中的 tss-lib 集成测试执行。该测试不将本地演示结果表述为生产安全证明或完整协议容量结论。
